// Placeholder until the app is actually deployed (deployment is deferred —
// see CLAUDE.md). Update once a real URL exists; used by metadata,
// sitemap.ts and robots.ts.
export const SITE_URL = "https://repere.example.com";
export const SITE_NAME = "Repère";
